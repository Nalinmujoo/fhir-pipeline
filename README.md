# FHIR Data Pipeline

A backend data pipeline built with Spring Boot. It reads patient data from a CSV file, transforms it into the official FHIR R4 JSON standard, and saves it to a PostgreSQL database, with all functionality accessible through secure REST APIs.